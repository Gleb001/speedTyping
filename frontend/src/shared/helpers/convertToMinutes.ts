// main ====================================================== //
// milliseconds -> minutes
const convertToMinutes = (time: number) => time / 60000;

// export ==================================================== //
export default convertToMinutes;