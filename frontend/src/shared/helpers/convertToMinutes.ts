// main ====================================================== //
const convertToMinutes = (time: number) => ((time / 1000) / 60);

// export ==================================================== //
export default convertToMinutes;